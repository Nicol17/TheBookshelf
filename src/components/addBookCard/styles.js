import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  addBookCard: {
    textAlign: 'center',
    display: 'block',
    width: '20%',
    height: 'auto',
    margin: '5% auto'
  },
  AddIcon: {
            display: 'block',
            textAlign: 'center',
            margin: '0 auto',
            fontSize: '100px', 
          },
}));